\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{wx672a}

\RequirePackage[margin=3cm,headheight=15pt]{geometry} % must before fancyhdr

\RequirePackage{wx672common}
\RequirePackage{wx672fonts}
\RequirePackage{wx672bib}

\RequirePackage{enumitem} % \setlist % conflict with beamer
\setlist{nosep} % kills all vertical spacing

\RequirePackage{indentfirst}
\setlength{\parindent}{2em}

\RequirePackage[bottom]{footmisc} % stick footnote at bottom of page

\RequirePackage{parskip} % not sure if this is needed

\RequirePackage[onehalfspacing]{setspace}% 
\renewcommand{\doublespacing}{\setstretch{1.6}}
\renewcommand{\singlespacing}{\setstretch{1.2}}
\renewcommand{\onehalfspacing}{\setstretch{1.4}}

% \overfullrule=2cm % for easy finding the overfull lines

% https://tex.stackexchange.com/questions/247185/tcolorbox-theorem-which-isnt-framed-on-the-sides
\RequirePackage[breakable]{tcolorbox}
\newtcolorbox{mainbodybox}[1]{breakable,sharp corners,
  colback=white, coltitle=black,colbacktitle=white,
  boxrule=.5pt,titlerule=0pt, 
  fonttitle=\bfseries,title=#1}

\RequirePackage{fancyhdr}
\pagestyle{fancy}

%\RequirePackage[svgnames]{xcolor} % must before hyperref
%\definecolor{Green}{cmyk}{1,0,1,.4}

\RequirePackage{wx672hyperref}

\DeclareOption{cn}{
  \renewcommand{\refname}{参考文献} % article.cls
  \renewcommand{\abstractname}{摘\hspace{1em}要}
}
\ProcessOptions\relax
