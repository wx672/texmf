\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{tikz-wx672}

\RequirePackage{latexsym,pifont,units,amsmath,amsfonts,amssymb,marvosym,mathtools}
\RequirePackage{xltxtra} %fontspec,xunicode are loaded here.
\defaultfontfeatures{Mapping=tex-text}
\setsansfont{DejaVu Sans}
%\setmainfont{DejaVu Serif}
\setmainfont{Humor Sans}

\newfontfamily\humor{Humor Sans}
\newfontfamily\purisa{Purisa}
\newfontfamily\dejavu{DejaVu Sans Mono}

% \RequirePackage{xeCJK}
% \setCJKmainfont[BoldFont={Adobe Heiti Std}]{Adobe Song Std}
% \setCJKsansfont[BoldFont={Adobe Heiti Std}]{Adobe Song Std}
% \setCJKmonofont{Noto Sans Mono CJK SC Regular}
% \setCJKfamilyfont{hei}{Adobe Heiti Std}
% \setCJKfamilyfont{song}{Adobe Song Std}
% \setCJKfamilyfont{kai}{Adobt Kaiti Std} % e.g. \CJKfamily{kai}
% \setCJKfamilyfont{xingkai}{FZXingKai-S04S}
% \newCJKfontfamily\quotefont{Adobt Kaiti Std}

% adjust the spacing between Chinese and English characters
% \xeCJKsetup{xCJKecglue=\hskip 0pt plus 0.08\baselineskip}

\RequirePackage[svgnames]{xcolor}

\RequirePackage{tikz}
\RequirePackage{tikz-penciline}

\usetikzlibrary{%
  external,%
  calc,positioning,%
  arrows,shapes,patterns,quotes,%
  decorations.pathmorphing,%
  backgrounds, fit,%
  trees,%
}%

\tikzexternalize[figure list=false, prefix=figs/]
\tikzset{external/up to date check=diff}
\tikzset{external/system call={xelatex \tikzexternalcheckshellescape%
    -interaction=batchmode -jobname "\image" "\texsource"}}

\tikzset{%
  help lines/.style = {ultra thin,dotted},%
  xy/.style = {font=\dejavu\tiny,inner sep=1pt}
}
% \tikzset{%
%   name node/.style={rectangle,draw,inner sep=3pt,node distance=0pt,minimum width=2.8em},%
%   tbox/.style={rectangle,inner sep=0pt,node distance=0pt,scale=.7,align=left},%
%   every label/.style={scale=.7,inner sep=0pt},%
%   tuli/.style={rectangle,inner sep=2pt,node distance=.1ex,scale=.6,align=left},%
% }

% \usepackage{minted}
% \usemintedstyle{bw}

