\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{wx672h}

\RequirePackage{ifxetex} % This class is for XeTeX only

\RequirePackage[noxcolor]{beamerarticle}% load xcolor later

\RequirePackage[top=2cm,bottom=2cm,left=3cm,right=2cm,%
nomarginpar,headheight=15pt,% to avoid "headheight too small"
%showframe,showcrop,
]{geometry}

\RequirePackage[svgnames]{xcolor}
\RequirePackage{wx672common}
\RequirePackage{wx672fonts}
\RequirePackage{wx672tikz}
\RequirePackage{wx672hyperref}
\RequirePackage{wx672bib}
\RequirePackage{wx672minted}
\usemintedstyle{bw}

\RequirePackage{fixme}
\fxsetup{
  margin=false,
  status=draft,
  author={\dejavu ⚠},
  inline=true,
  marginclue=true,
  theme=color
}

\RequirePackage{enumitem} % \setlist
\setlist{nosep} % kills all vertical spacing

\addtolength{\oddsidemargin}{-15pt}
\addtolength{\marginparsep}{5pt}
\newcommand{\Marginpar}[1]{\marginpar{\raggedright{\footnotesize \emph{#1}}}}

\renewcommand{\refname}{} % article.cls
\renewcommand{\contentsname}{Contents}
\renewcommand{\listfigurename}{Figures}
\renewcommand{\listtablename}{Tables}
\renewcommand{\figurename}{Fig.}
\renewcommand{\tablename}{Tbl.}
\renewcommand{\appendixname}{App.}
\renewcommand{\indexname}{Idx.}

% defined in wx672common
\renewcommand{\Navy}[1]{\textcolor{black}{#1}}
\newcommand\code[1]{{\ttfamily #1}}

\graphicspath{{./figs/}{../figs/}} %note that the trailing “/” is required

\setcounter{tocdepth}{2}

\newenvironment{iblock}[1]
{\setbeamercolor{block title}{bg=white}\setbeamercolor{block body}{bg=white}\begin{block}{#1}}
  {\end{block}}